{
  "name": "ekocep",
  "private": true,
  "version": "1.0.0",
  "description": "Static HTML, Tailwind CSS and JavaScript storefront.",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css script.js dist/ && cp -R public dist/public",
    "dev": "python3 -m http.server 4173",
    "start": "python3 -m http.server 4173"
  }
}
